# Tina CMS credentials — get these from https://app.tina.io after creating a project
NEXT_PUBLIC_TINA_CLIENT_ID=your-tina-client-id-here
TINA_TOKEN=your-read-only-token-here # keep private

# Optional: override the branch Tina uses (defaults to "main")
# TINA_BRANCH=main
