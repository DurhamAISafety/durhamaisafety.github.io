---
import SupportersGrid from '../tina-islands/SupportersGrid.astro';
import type { Supporter } from '../../data/supporters';

interface Props {
  supporters: Supporter[];
}

const { supporters } = Astro.props;
---

<div class="contents" data-tina-island="/tina-island/supporters-grid">
    <SupportersGrid supporters={supporters} />
</div>
